@model CategoryListViewModel


<h1>Kategoriler</h1>

<div class="list-group">

    <a href="/product/index" class="list-group-item">Tüm Ürünler</a>

    @foreach (var category in Model.Categories)
    {
        var css = "list-group-item";
        if(category.CategoryId == Model.CurrentCategory)
        {
            css += " active";   //bu diyorki gelen id sıradaki id ye eşitse class kısmına active ekliyor.
        }
        <a href="/product/index?category=@category.CategoryId" class="@css">@category.CategoryName</a>
    }
</div>

